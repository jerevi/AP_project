from objects.instance import Instance


class Ordo:
    def __init__(self, instance, time):
        self.problem = instance
        self.time = time

    @staticmethod
    def define_realisable():
        if self.problem.:
            # On stock le couple (tache / job) dans les taches possible
            tache_possible.append(i)
